# ============================================
# 后端环境变量配置示例
# ============================================
# 复制此文件为 .env 并填写实际值
# cp env.example .env

# ============================================
# 数据库配置
# ============================================
# PostgreSQL 数据库连接字符串
# 格式: postgresql://用户名:密码@主机:端口/数据库名
# 示例: postgresql://postgres:password@localhost:5432/inventory_db
DATABASE_URL=postgresql://postgres:password@localhost:5432/inventory_db

# ============================================
# 服务器配置
# ============================================
# 服务器监听地址（0.0.0.0 表示监听所有网络接口）
HOST=0.0.0.0

# 服务器端口
PORT=8000

# ============================================
# CORS 配置
# ============================================
# 允许跨域请求的前端地址（多个地址用逗号分隔）
# 开发环境示例:
# CORS_ORIGINS=http://localhost:3000,http://localhost:5173
# 
# 生产环境示例（包含 Vercel 域名）:
# CORS_ORIGINS=https://your-app.vercel.app,https://yourdomain.com
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ============================================
# JWT 认证配置
# ============================================
# ⚠️ 重要：必须更改此密钥！使用默认值会导致应用无法启动
# 
# 生成强随机密钥的方法：
# python -c "import secrets; print(secrets.token_urlsafe(32))"
# 
# 密钥要求：
# - 至少 32 个字符
# - 使用随机生成的字符串
# - 不要使用可预测的值
# - 生产环境必须使用强密钥
JWT_SECRET=your-secret-key-change-in-production

# JWT 算法（通常不需要修改）
JWT_ALGORITHM=HS256

# JWT Token 过期时间（分钟）
# 默认 30 分钟，可根据安全需求调整
JWT_EXPIRE_MINUTES=30

